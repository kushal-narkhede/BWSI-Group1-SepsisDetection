# Everything in the app, plus what the notebooks and training scripts need.
# Use this locally; the deployed app installs requirements.txt only.

-r requirements.txt

matplotlib
seaborn
jupyter
ipykernel

# CATBOOST_MEDLYTICS.ipynb jits its rolling-slope feature with numba. Training only --
# the app never recomputes it, so this stays out of requirements.txt.
numba
